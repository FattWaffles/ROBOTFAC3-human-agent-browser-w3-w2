{
  "name": "robotfac3-prototype",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "Zero-dependency prototype. This file exists only so Node treats the tests as ES modules. There is nothing to install.",
  "scripts": {
    "start": "python3 relay.py",
    "test": "node --test tests/"
  }
}
