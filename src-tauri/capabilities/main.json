{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Only the RobotFac3 UI window may call the Rust core. Website windows get no IPC at all.",
  "windows": ["main"],
  "permissions": ["allow-rpc", "allow-evm-rpc", "allow-rpc-info", "allow-open-site"]
}
